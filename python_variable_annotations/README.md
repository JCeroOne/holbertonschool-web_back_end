# Python: Variable annotations
**Python** is a dynamically-typed language, which means that variable types are determined as the program is run, when a value gets assigned to them.

Type annotations help developers to know what type each variable is when reading the code, and also allows for code validators to catch any issues regarding types before the code itself is executed.
