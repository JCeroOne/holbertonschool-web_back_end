#!/usr/bin/env python3
import math

"""Defines the 'floor' function."""


def floor(n: float) -> int:
    """Removes the decimal places of a number.

    Parameters:
        n (float): The number to truncate.

    Returns: (int) The truncated number."""

    return math.floor(n)
