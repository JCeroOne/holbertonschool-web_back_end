#!/usr/bin/env python3
"""Defines the 'to_str' function."""


def to_str(n: float) -> str:
    """Returns the string representation of a number.

    Parameters:
        n (float): A number.

    Returns: (str) The string representation of the number."""

    return str(n)
